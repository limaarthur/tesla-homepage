import styled from "styled-components";

export const Container = styled.div`
  height: 100vh; // Cada carro irá ocupar 100% de altura da tela

  scroll-snap-align: start; // Define o alinhamento do scroll para o elemento pai
`;